name = "eddie_frontend"
version = "0.1.0"
target = "javascript"
description = "Eddie frontend — Lustre SPA"

[dependencies]
gleam_stdlib = ">= 0.59.0 and < 1.0.0"
gleam_json = ">= 3.1.0 and < 4.0.0"
lustre = ">= 5.6.0 and < 6.0.0"
lustre_websocket = ">= 0.9.0 and < 1.0.0"
modem = ">= 2.0.0 and < 3.0.0"
eddie_shared = { path = "../shared" }

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
