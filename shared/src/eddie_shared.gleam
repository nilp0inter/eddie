/// Shared types and codecs for the Eddie agent protocol.
///
/// This package defines the domain types that cross the WebSocket boundary
/// between the backend (Erlang) and frontend (JavaScript) applications.
/// It contains no platform-specific code and compiles to both targets.
pub fn placeholder() -> Nil {
  Nil
}
